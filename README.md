# Установщик сервера Matrix Synapse для Ubuntu/Debian

Этот скрипт автоматически разворачивает полнофункциональный сервер Matrix (Synapse) на чистом сервере Ubuntu или Debian. Идеально подходит для создания частного, защищенного мессенджера для семьи, друзей или небольшой компании.

## ✨ Возможности

- **Matrix Synapse:** Основной сервер для сообщений.
- **PostgreSQL:** Производительная база данных вместо медленной SQLite.
- **Nginx:** Веб-сервер в качестве безопасного обратного прокси.
- **Let's Encrypt:** Автоматическое получение и обновление SSL-сертификата (HTTPS).
- **Coturn:** TURN-сервер для стабильных аудио- и видеозвонков, работающих даже за NAT.
- **Firewall:** Автоматическая настройка `ufw` для безопасности.

## 📋 Требования

1.  **Чистый сервер (VPS)** с **Ubuntu 20.04/22.04** или **Debian 11/12**.
2.  **Доменное имя**, направленное на IP-адрес вашего сервера (A-запись и SRV-запись).
3.  Доступ к серверу с правами `root`.

### Настройка DNS
Перед запуском скрипта убедитесь, что в панели управления вашим доменом созданы записи:

1.  **A-запись:**
    - **Тип:** `A`
    - **Имя:** `@` (или ваш домен)
    - **Значение:** IP-адрес вашего сервера

2.  **SRV-запись:**
    - **Тип:** `SRV`
    - **Service:** `_matrix`
    - **Protocol:** `_tcp`
    - **Port:** `8448`
    - **Target:** `@` (или ваш домен)

## 🚀 Установка

1.  **Клонируйте репозиторий:**
    ```
    git clone https://github.com/ВАШ_НИК/ВАШ_РЕПОЗИТОРИЙ.git
    cd ВАШ_РЕПОЗИТОРИЙ
    ```
    Или просто скачайте `install_matrix.sh` и загрузите его на сервер.

2.  **Настройте скрипт:**
    Откройте файл `install_matrix.sh` в любом редакторе и измените переменные вверху:
    ```
    DOMAIN="your-domain.com"
    EMAIL="your-email@example.com"
    # Опционально: настройте белый список для федерации
    WHITELIST_DOMAINS=("friend-server.com")
    ```

3.  **Запустите установку:**
    ```
    bash install_matrix.sh
    ```
    Скрипт выполнит все шаги автоматически. Это может занять 5-10 минут.

## 🛠️ После установки

После завершения скрипта вам нужно создать первого пользователя:

1.  Выполните команду, которую предложит скрипт:
    ```
    register_new_matrix_user -c /etc/matrix-synapse/homeserver.yaml http://localhost:8008
    ```
2.  Придумайте логин, пароль и **обязательно** ответьте `yes` на вопрос `Make admin?`.

3.  Скачайте приложение [Element](https://element.io/download), выберите "Использовать свой сервер" и введите адрес `https://your-domain.com`.

## 📜 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для подробностей.
```

Замените `ВАШ_НИК/ВАШ_РЕПОЗИТОРИЙ` на реальный путь.

### Что в итоге?
Ваш репозиторий будет выглядеть профессионально, содержать исчерпывающую, но простую инструкцию, и любой желающий сможет легко им воспользоваться.
